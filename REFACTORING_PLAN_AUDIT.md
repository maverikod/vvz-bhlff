# План рефакторинга после аудита кода

## 🚨 КРИТИЧЕСКИЕ НАРУШЕНИЯ СТАНДАРТОВ ПРОЕКТА

### 1. Файлы превышающие лимит 400 строк

**КРИТИЧНО**: Найдено 4 файла, превышающих лимит 400 строк:

1. **`./tests/unit/test_core/test_time_integrators.py`** - 537 строк
2. **`./tests/unit/test_core/test_time_integrators_physics.py`** - 424 строки  
3. **`./bhlff/core/fft/spectral_operations.py`** - 475 строк
4. **`./bhlff/core/fft/fft_solver_7d.py`** - 493 строки

### 2. Недописанный код

**КРИТИЧНО**: Найдены множественные нарушения:

#### 2.1. `pass` в неабстрактных методах
- **`./bhlff/core/time/base_integrator.py`** - 2 места
- **Множественные тестовые файлы** - заглушки вместо реализации

#### 2.2. `NotImplementedError` в неабстрактных методах
- **`./bhlff/core/fft/spectral_operations.py`** - 9 методов с NotImplementedError
- **`./bhlff/core/fft/fft_solver_7d.py`** - TODO комментарии

#### 2.3. Упрощения и заглушки
- **`./tests/integration/test_bvp_core_pipeline_physics.py`** - "simplified" алгоритм
- **Множественные TODO** - недописанная функциональность

## 📋 ПЛАН ИСПРАВЛЕНИЙ

### Этап 1: Разделение больших файлов

#### 1.1. `spectral_operations.py` (475 строк)
**Разделить на:**
- `spectral_operations.py` - основной класс (≤350 строк)
- `spectral_derivatives.py` - производные (≤200 строк)
- `spectral_filtering.py` - фильтрация (≤200 строк)

#### 1.2. `fft_solver_7d.py` (493 строки)
**Разделить на:**
- `fft_solver_7d.py` - основной класс (≤350 строк)
- `fft_solver_time.py` - временные методы (≤200 строк)
- `fft_solver_validation.py` - валидация (≤200 строк)

#### 1.3. Тестовые файлы
**Разделить на:**
- `test_time_integrators_basic.py` - базовые тесты (≤300 строк)
- `test_time_integrators_advanced.py` - продвинутые тесты (≤300 строк)
- `test_time_integrators_physics_basic.py` - базовые физические тесты (≤300 строк)
- `test_time_integrators_physics_advanced.py` - продвинутые физические тесты (≤300 строк)

### Этап 2: Реализация недописанного кода

#### 2.1. `spectral_operations.py`
**Реализовать методы:**
- `compute_spectral_derivative()` - спектральные производные
- `apply_spectral_filter()` - спектральная фильтрация
- `compute_spectral_gradient()` - спектральный градиент
- `compute_spectral_divergence()` - спектральная дивергенция
- `compute_spectral_curl()` - спектральный ротор

#### 2.2. `base_integrator.py`
**Реализовать методы:**
- `validate_inputs()` - валидация входных данных
- `compute_time_step()` - вычисление временного шага

#### 2.3. Тестовые файлы
**Реализовать все заглушки:**
- Заменить все `pass` на реальные тесты
- Реализовать физические проверки
- Добавить валидацию алгоритмов

### Этап 3: Устранение упрощений

#### 3.1. Убрать "simplified" алгоритмы
- Реализовать полные алгоритмы вместо упрощенных
- Добавить все необходимые проверки
- Убрать заглушки и TODO

#### 3.2. Реализовать недостающую функциональность
- Спектральные производные
- Спектральная фильтрация
- Валидация решений
- Обработка граничных условий

### Этап 4: Проверка соответствия теории

#### 4.1. Сверка с ТЗ
- Проверить все алгоритмы на соответствие ТЗ
- Убедиться в правильности физических формул
- Проверить численную точность

#### 4.2. Сверка с теорией
- Проверить соответствие 7D BVP теории
- Убедиться в правильности спектральных операций
- Проверить временные интеграторы

## 🎯 ПРИОРИТЕТЫ

### Высокий приоритет (КРИТИЧНО)
1. **Разделение больших файлов** - нарушение основного правила проекта
2. **Реализация NotImplementedError** - недописанный код
3. **Устранение pass в неабстрактных методах** - заглушки

### Средний приоритет
1. **Реализация спектральных операций** - недостающая функциональность
2. **Улучшение тестов** - качество тестирования

### Низкий приоритет
1. **Оптимизация производительности** - после исправления критических ошибок
2. **Документация** - после реализации кода

## 📊 МЕТРИКИ КАЧЕСТВА

### До рефакторинга
- **Файлов > 400 строк**: 4
- **NotImplementedError**: 9
- **pass в неабстрактных методах**: 30+
- **TODO комментарии**: 5+

### После рефакторинга (цель)
- **Файлов > 400 строк**: 0
- **NotImplementedError**: 0 (кроме абстрактных)
- **pass в неабстрактных методах**: 0
- **TODO комментарии**: 0

## 🔧 ИНСТРУМЕНТЫ ПРОВЕРКИ

### Автоматические проверки
```bash
# Проверка размера файлов
find . -name "*.py" -not -path "./.venv/*" -exec wc -l {} + | awk '$1 > 400 {print "ERROR: " $2 " has " $1 " lines"}'

# Проверка на pass
grep -r "pass$" . --include="*.py" | grep -v "abstract" | grep -v ".venv"

# Проверка на NotImplemented
grep -r "NotImplemented" . --include="*.py" | grep -v "abstract" | grep -v ".venv"

# Проверка на упрощения
grep -r "simplified\|simplify\|placeholder\|TODO\|FIXME" . --include="*.py" | grep -v ".venv"
```

### Ручные проверки
- [ ] Все файлы содержат докстринг с автором и email
- [ ] Все классы имеют подробные докстринги с физическим смыслом
- [ ] Все методы описаны с указанием физического смысла
- [ ] Нет использования `pass` или `NotImplemented` в неабстрактных методах
- [ ] Размер всех файлов не превышает 400 строк
- [ ] Конфигурации используют только JSON формат
- [ ] Именование соответствует стандартам (snake_case, PascalCase)

## 📝 ЗАКЛЮЧЕНИЕ

Обнаружены **критические нарушения** стандартов проекта:
- **4 файла превышают лимит** 400 строк
- **Множественные NotImplementedError** в неабстрактных методах
- **30+ заглушек pass** вместо реализации
- **Упрощенные алгоритмы** вместо полной реализации

**Требуется немедленный рефакторинг** для приведения кода в соответствие со стандартами проекта.
