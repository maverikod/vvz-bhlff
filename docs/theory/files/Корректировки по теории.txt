Принял. Я изучил присланный материал с правками по **закону подобия** и сопоставил его с текущим каноном Θ-теории. Ниже — **строгий результат в нужном формате**:

1. где именно в теории есть принципиальные расхождения;
2. какие главы/блоки требуют корректировки;
3. **готовые корректирующие блоки**, которые можно прямо вставлять в корпус теории.

Источник правок: файл `7d-113-Закон_Подобия_правки.md` 

---

## I. Что именно изменилось принципиально

В текущей версии теории **закон подобия трактовался феноменологически** — как почти масштабная самоподобность объектов/уравнений.
В новой редакции (по твоему материалу) он **онтологически переопределён**:

### Было (устарело)

* подобие = масштабное копирование структур;
* уровни как почти повторяющиеся «объекты»;
* масштаб как квази-онтологическая величина;
* переходы между уровнями — сглаженные.

### Стало (канон)

* **подобие = инвариантность Θ-паспорта**, а не формы;
* уровни = **редкие дефекты-стционары**;
* масштаб — *производный параметр*, не первичный;
* переходы **строго ступенчатые** через Σ-лестницу;
* ключевая ось — *(Σ-class, νℚ, Q, σ)*, а не размер.

Это означает: **ряд уже написанных глав логически неверны**, даже если формулы локально корректны.

---

## II. Где именно в теории нужны правки (карта поражения)

### 1. Блоки, затрагивающие «закон подобия» напрямую

**Критические (обязательная правка):**

* **7d-113** — закон подобия (переписать концептуально);
* **7d-105** — уровни и укрупнение (уточнить: не масштаб, а паспорт);
* **7d-106** — ансамбли и фазовые состояния (убрать остаточную феноменологию);
* **7d-99** — S/G-процессы и квенчи (явно ввести Σ-лестницу как обязательную).

**Системные (правка формулировок):**

* **7d-97** — иерархия резонаторов (убрать «самоподобие форм»);
* **7d-86** — заморозка/квазистационарность (привязать к νℚ и Q);
* **7d-71** — галактика C1–C3 (подчеркнуть: проекция мод, не геометрическое подобие).

---

## III. Корректирующие блоки (готовы к вставке)

Ниже — **три канонических корректирующих блока**, оформленные в стиле теории.

---

### 🔧 Корректирующий блок K-113

**(Замена феноменологического закона подобия)**

> **Правка к 7d-113**

**Старую формулировку закона подобия считать недействительной.**

#### Новая формулировка (канон)

**Закон Θ-подобия**

> Подобие между уровнями определяется **инвариантностью Θ-паспорта**, а не геометрическим, масштабным или функциональным сходством наблюдаемых структур.

Формально:
[
\mathcal P^{(n)}*{\Theta}
;\cong;
\mathcal P^{(m)}*{\Theta}
\quad\Longleftrightarrow\quad
\begin{cases}
\Sigma\text{-class}^{(n)} = \Sigma\text{-class}^{(m)} \
\sigma^{(n)} = \sigma^{(m)} \
\nu_{\mathcal Q}^{(n)} \sim \nu_{\mathcal Q}^{(m)} \
Q^{(n)} \sim Q^{(m)}
\end{cases}
]

Масштаб, масса, длины и времена **не входят** в критерий подобия и являются производными наблюдаемыми.

---

### 🔧 Корректирующий блок K-105

**(Уточнение понятия уровня и укрупнения)**

> **Правка к 7d-105**

**Уровень ≠ масштаб.**

Уровень — это **дефект-стационар Θ-поля**, возникающий **только после прохождения всей Σ-лестницы ансамбля**.

#### Каноническое условие уровня

[
\exists,\Delta t_*:
\quad
\Sigma \to \Sigma^*,
;
\nu_{\mathcal Q}\downarrow,
;
Q\uparrow,
;
N_{\text{mode}}\to 1
]

Следствие:

* атомы и молекулы — **ансамбли**, а не уровни;
* плазма — обязательная фильтрующая стадия;
* прямые переходы «газ → ядро» запрещены.

---

### 🔧 Корректирующий блок K-106

**(Ансамбли и фазовые состояния — жёсткая паспортная форма)**

> **Правка к 7d-106**

Термин «ансамбль» допускается **только** в паспортной форме.

#### Θ-паспорт ансамбля

[
\mathcal P_A
============

(\kappa,;Q,;\varepsilon,;\sigma,;\nu_{\mathcal Q},;\Pi,;\Sigma\text{-class})
]

Агрегатные состояния (кристалл, жидкость, газ, плазма) — это **не сущности**, а **Σ-классы** ансамбля.

| Σ-класс | Физический смысл    |
| ------- | ------------------- |
| Σ-1     | жёсткая связность   |
| Σ-2     | текучая доменная    |
| Σ-3     | разреженная         |
| Σ-4     | плазменная (фильтр) |
| Σ-5     | дефект-уровень      |

---

## IV. Итог (коротко и жёстко)

1. Закон подобия **перешёл из геометрии в онтологию**.
2. Масштаб **десакрализован**: он не первичен.
3. Все рассуждения о «самоподобии форм» — **устаревшие**.
4. Подобие = **одинаковый Θ-паспорт**, а не одинаковый вид.
5. Уровни редки и возникают **только через плазменную фильтрацию**.

---

Если хочешь, следующим шагом я могу:

* сделать **полную карту покрытий**: какие формулы теперь нарушают новый закон;
* или оформить это как **единую корректирующую статью** для вставки в базу (`K-Law-Similarity.md`).


Хорошо. Ниже — **двухчастный результат**, строго в проектном стиле:

1. **Полная карта покрытий** по теории после изменения закона подобия
2. **Корректировочная глава**, написанная *на основе карты*, а не «с потолка»

Без феноменологии, без масштабной интуиции, только Θ-онтология.

---

# I. Полная карта покрытий по закону подобия

Карта строится относительно **нового канона**:

> **Подобие = инвариантность Θ-паспорта**,
> масштаб, размер, масса — вторичны и не онтологичны.

## I.1. Критерии проверки покрытия

Для каждого блока проверяется наличие / отсутствие:

1. **Паспортной формулировки** (σ, Σ-class, Q, νℚ, Π)
2. **Ступенчатости (Σ-лестницы)**, а не непрерывности
3. **Запрета прямых переходов**
4. **Разделения: уровень ↔ ансамбль**
5. **Отсутствия масштабной онтологии**

---

## I.2. Таблица покрытий (ядро)

### 🔴 Критические расхождения (обязательная правка)

| Блок       | Статус        | Проблема                                             |
| ---------- | ------------- | ---------------------------------------------------- |
| **7d-113** | ❌ отсутствует | Подобие трактуется как масштабная аналогия           |
| **7d-105** | ⚠ частично    | Уровень описан корректно, но укрупнение ещё «похоже» |
| **7d-106** | ⚠ частично    | Ансамбль введён, но не жёстко паспортирован          |
| **7d-99**  | ⚠ частично    | ℚ-динамика есть, Σ-лестницы нет как закона           |

---

### 🟡 Системные расхождения (терминологические)

| Блок       | Статус | Проблема                                           |
| ---------- | ------ | -------------------------------------------------- |
| **7d-97**  | ⚠      | Самоподобие резонаторов читается геометрически     |
| **7d-86**  | ⚠      | «Заморозка» без явной связи с Q и νℚ               |
| **7d-71**  | ⚠      | C1–C3 можно прочитать как геометрию, а не проекцию |
| **7d-103** | ⚠      | Макрофон не связан явно с паспортом                |

---

### 🟢 Совместимые блоки (правка не требуется)

| Блок  | Статус | Комментарий                     |
| ----- | ------ | ------------------------------- |
| 7d-93 | ✅      | Чистая дефектная онтология      |
| 7d-90 | ✅      | Паспорт ядра корректен          |
| 7d-88 | ✅      | Без масштабных аксиом           |
| 7d-82 | ✅      | Локальная жёсткость без подобия |

---

## I.3. Ключевой вывод карты

**Главная проблема теории сейчас не в формулах, а в логике переходов.**

Формулы часто корректны,
но **читаются в старом (масштабно-аналоговом) смысле**.

➡️ Значит нужна **одна сквозная корректирующая глава**,
которая:

* переопределяет закон подобия;
* фиксирует Σ-лестницу как *единственный* механизм укрупнения;
* запрещает все «похожие, но не паспортные» аргументы.

---

# II. Корректировочная глава

## «Закон Θ-подобия и ступенчатая структура уровней»

*(вставка после 7d-113, с ретро-действием на всю теорию)*

---

## II.1. Методологическое замечание

Во всех предыдущих версиях теории использовался термин «подобие»
в смысле **внешнего сходства структур**.

Это допускало неверное чтение:

* уровни как масштабные копии;
* укрупнение как непрерывный процесс;
* массу и размер как признаки уровня.

Настоящая глава **аннулирует такую интерпретацию**.

---

## II.2. Каноническое определение Θ-подобия

### Определение 1 (Θ-подобие)

Два объекта Θ-онтологии считаются подобными **тогда и только тогда**, когда их **Θ-паспорта изоморфны**.

[
\mathcal P_{\Theta}
===================

(\kappa,;Q,;\varepsilon,;\sigma,;\nu_{\mathcal Q},;\Pi,;\Sigma\text{-class})
]

Ни один геометрический или масштабный параметр
**не входит** в критерий подобия.

---

## II.3. Уровень, ансамбль и запрет масштабной иерархии

### Определение 2 (Уровень)

**Уровень** — это стационарный дефект Θ-поля,
возникающий при выполнении условия застёжки:

[
\Sigma \to \Sigma^*,\quad
\nu_{\mathcal Q}\downarrow,\quad
Q\uparrow,\quad
N_{\text{mode}}\to1
]

Все объекты, не удовлетворяющие этому условию,
являются **ансамблями**, независимо от масштаба.

---

## II.4. Σ-лестница как единственный механизм укрупнения

### Аксиома 1 (Ступенчатость)

Переход от ансамбля к уровню возможен **только** через последовательность Σ-классов:

[
\Sigma_1
;\rightarrow;
\Sigma_2
;\rightarrow;
\Sigma_3
;\rightarrow;
\Sigma_4
;\rightarrow;
\Sigma_5
]

Где:

* Σ₁ — жёсткая связность
* Σ₂ — доменная текучесть
* Σ₃ — разреженность
* Σ₄ — плазменный фильтр
* Σ₅ — дефект-уровень

**Прямые переходы запрещены.**

---

## II.5. Следствия для всей теории

### Следствие 1

Масштаб **не может** использоваться как аргумент подобия.

### Следствие 2

Фразы вида «на другом масштабе то же самое»
корректны **только** при совпадении Θ-паспорта.

### Следствие 3

Атом, звезда и галактика подобны
**не потому что “похожи”**,
а потому что реализуют **один и тот же паспорт уровня**.

### Следствие 4

Все агрегатные состояния вещества —
это **Σ-классы ансамблей**, а не уровни.

---

## II.6. Ретро-коррекция чтения предыдущих глав

Все упоминания:

* самоподобия,
* масштабной аналогии,
* «того же самого, но больше»

следует **перечитывать** как:

> «реализация того же Θ-паспорта
> в другой проекции Π и при других значениях κ»

---

## II.7. Финальный тезис главы

> **Θ-теория не масштабна.
> Она паспортна.
> Уровни не растут — они собираются.**

---

# III. Что дальше (логически)

После этой главы **обязательно**:

1. переписать 7d-113 как ссылочный узел;
2. пройтись по формулам в 7d-105 / 7d-99 и убрать «плавность»;
3. ввести **паспортную проверку** везде, где упоминается подобие.

Если хочешь, следующим шагом я могу:

* пройтись **по формулам** и пометить, где чтение неверно;
* или оформить это как **единый корректирующий коммит-блок** для базы SQLite (ID + зависимости).


